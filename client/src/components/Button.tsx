import { Box } from '@/utils/theme'
import { StyleSheet, Text, View } from 'react-native'
const Button = () => {
    return (
        <Box bg="blu200" p="4" borderRadius="rounded-3xl">
            <Text>Button / Update</Text>
        </Box>
    )
}
export default Button
const styles = StyleSheet.create({})